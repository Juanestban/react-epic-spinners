import { AtomSpinner } from './AtomSpinner/AtomSpinner'
import { BreedingSpinner } from './BreedingSpinner/BreedingSpinner'

const Spinners = {
  AtomSpinner,
  BreedingSpinner,
}

const nameSpinners = ['Atom spinner', 'Breeding Rhombus spinner']

export default Spinners
export { AtomSpinner, BreedingSpinner, nameSpinners }
